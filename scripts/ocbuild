# Runs a build for the specified application in your
# current open shift project using the specified local
# directory.
# Usage:
#   ocbuild <application name> <targetdir>
# Example:
#   ocbuild railsapp ..
appname=$1
targetdir=$2
cp -r assemble_configs $targetdir/tmp/
oc start-build -F $appname --from-dir=$targetdir
#oc get builds --watch
